# Geometry

The baseline geometry is defined directly in `case/system/blockMeshDict`.

Use this directory for future CAD exports from NX, neutral geometry such as STEP files, or documented simplifications used to create OpenFOAM-ready surfaces.
